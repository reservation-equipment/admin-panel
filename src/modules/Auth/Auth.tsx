import React from 'react';
import AuthForm from "./components/AuthForm/AuthForm.tsx";

const Auth = () => {
    return (
        <div>
            <AuthForm/>
        </div>
    );
};

export default Auth;
