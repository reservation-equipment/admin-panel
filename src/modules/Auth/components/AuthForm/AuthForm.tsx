import React from 'react';
import TextInput from "../../../../shared/UI/TextInput/TextInput.tsx";

const AuthForm = () => {
    return (
        <form>
            <TextInput wrapperStyle={} inputStyle={} value={} placeholder={}/>
        </form>
    );
};

export default AuthForm;
