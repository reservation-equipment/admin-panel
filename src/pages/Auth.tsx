import AuthForm from "@src/widgets/auth-form/AuthForm.tsx";

const Auth = () => {
  return (
    <div className={"flex w-full h-screen items-center justify-center"}>
      <AuthForm />
    </div>
  );
};


export default Auth;
